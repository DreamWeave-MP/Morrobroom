# Third-party source notices

The code under `src/slipgate/` incorporates parser and geometry source released
under **MIT OR Apache-2.0**.

The imported snapshots and their preservation commits are:

- parser snapshot: `9849048`
- geometry snapshot: `38d404c`

The original upstream license texts and source history remain available in the
repository's parser-import and geometry-import commits.
The original upstream copyright and attribution notices must be retained when
redistributing modified versions of this code.
